{
  "name": "mota-unified-scholarship-app",
  "version": "1.0.0",
  "description": "Unified Scholarship Mobile Application for Tribal Students - Ministry of Tribal Affairs (MoTA), Government of India",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run --prefix client build",
    "vercel-build": "npm install --prefix client && npm run --prefix client build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently --kill-others-on-fail -n \"SERVER,CLIENT\" -c \"bgBlue.bold,bgGreen.bold\" \"npm run dev:server\" \"npm run dev:client\"",
    "test:all": "cd server && node test_all_phases.js && node test_security_dpdp.js",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
